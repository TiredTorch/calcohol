{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/calcohol-user",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/calcohol-user",
        "outputPath": "dist/apps/calcohol-user"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/calcohol-user"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "calcohol-user:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "calcohol-user:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "calcohol-user:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "calcohol-user:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/calcohol-user"],
      "options": {
        "jestConfig": "apps/calcohol-user/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/calcohol-user/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
